{
  "name": "football-payment-root",
  "version": "1.0.0",
  "scripts": {
    "deploy": "cd web && npm run build && cd .. && firebase deploy",
    "firestore:indexes": "firebase firestore:indexes"
  },
  "private": true
}
